package bflow.budget.enums;

/**
 * Budget scope enumeration.
 */
public enum BudgetScope {
    /**
     * Budget scoped to a wallet.
     */
    WALLET,
    /**
     * Budget scoped to a category.
     */
    CATEGORY
}
